from setuptools import setup, find_packages

setup(
    name='binance-api-manager',
    version='0.1',
    description='A simple package to manage Binance API keys',
    author='Burak Toy',
    packages=find_packages(),
)

